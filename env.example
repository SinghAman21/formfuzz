## Copy to `.env.local` (recommended) or export in your shell before `npm run dev`.
##
## This value is used in a Client Component, so it MUST start with `NEXT_PUBLIC_`.
NEXT_PUBLIC_WEB_APP_URL="https://script.google.com/macros/s/REPLACE_ME/exec"


